-- Displays the number of records with id = 89 in first_table of the db hbtn_0c_0 in your MySQL server.
-- The database name will be passed as an argument of the mysql command

SELECT COUNT(*) 
	FROM first_table 
	WHERE id=89;
